{
    "name": "@media-router/plugin-rist-core",
    "version": "2.0.0",
    "private": true,
    "description": "Library plugin (no mediaRouter manifest — never appears in Add Module): RIST domain base code. librist.py ctypes bindings loaded by the gst pipeline runner for rist-input / rist-output."
}
