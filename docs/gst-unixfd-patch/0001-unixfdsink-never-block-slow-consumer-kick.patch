--- a/gst/unixfd/gstunixfdsink.c
+++ b/gst/unixfd/gstunixfdsink.c
@@ -51,6 +51,7 @@
 
 #include <stdint.h>
 #include <glib/gstdio.h>
+#include <sys/socket.h>
 #include <gio/gio.h>
 #include <gio/gunixsocketaddress.h>
 
@@ -70,8 +71,19 @@
 {
   GHashTable *buffers;
   GSource *source;
+  /* Local patch (media-router): monotonic time when this client's socket
+   * first failed the writability check; 0 while writable. Buffers are
+   * SKIPPED (not sent) for an unwritable client, and the client is only
+   * kicked once it has been continuously unwritable for KICK_AFTER_US —
+   * a slow-STARTING consumer (VA/librist init between connect and PLAYING)
+   * must not be killed, it just misses the data it wasn't reading anyway
+   * (the same loss UDP multicast gave it). */
+  gint64 blocked_since;
 } Client;
 
+/* How long a client may stay continuously unwritable before it is kicked. */
+#define KICK_AFTER_US (10 * G_USEC_PER_SEC)
+
 struct _GstUnixFdSink
 {
   GstBaseSink parent;
@@ -385,6 +397,22 @@
     return G_SOURCE_CONTINUE;
   }
 
+  /* Local patch (media-router): at datagram-like buffer rates (thousands of
+   * NEW_BUFFER commands/s) the default ~208KB socket buffer holds well under
+   * a second of commands — a client that pauses briefly (e.g. downstream
+   * preroll at join) overflows it and trips the slow-consumer kick. Give
+   * each client several seconds of headroom so only a genuinely hung
+   * consumer gets kicked (requires net.core.wmem_max >= this value). */
+  {
+    GError *sockopt_error = NULL;
+    if (!g_socket_set_option (client_socket, SOL_SOCKET, SO_SNDBUF,
+            4 * 1024 * 1024, &sockopt_error)) {
+      GST_WARNING_OBJECT (self, "Could not grow client send buffer: %s",
+          sockopt_error->message);
+      g_clear_error (&sockopt_error);
+    }
+  }
+
   client = g_new0 (Client, 1);
   client->buffers =
       g_hash_table_new_full (NULL, NULL, (GDestroyNotify) gst_buffer_unref,
@@ -440,6 +468,18 @@
 
   GST_OBJECT_LOCK (self);
 
+  /* Local patch (media-router): a SIGKILL'd producer leaves its socket file
+   * behind and the next bind fails with EADDRINUSE. Engine pipelines are
+   * restarted aggressively, so unlink a pre-existing path before binding —
+   * same "last bind wins" semantics the UDP bus had. */
+  if (self->socket_type == G_UNIX_SOCKET_ADDRESS_PATH
+      && self->socket_path != NULL
+      && g_file_test (self->socket_path, G_FILE_TEST_EXISTS)) {
+    GST_WARNING_OBJECT (self, "Removing stale socket path %s",
+        self->socket_path);
+    g_unlink (self->socket_path);
+  }
+
   self->socket =
       gst_unix_fd_socket_new (self->socket_path, self->socket_type, &addr,
       &error);
@@ -517,6 +557,34 @@
   g_hash_table_iter_init (&iter, self->clients);
   while (g_hash_table_iter_next (&iter, (gpointer) & socket,
           (gpointer) & client)) {
+    /* Local patch (media-router): never block in render. This runs under
+     * GST_OBJECT_LOCK; a blocking send to a full client socket deadlocks the
+     * whole sink — the command thread (which drains RELEASE_BUFFER messages
+     * and accepts new clients) waits on the same lock, so the client can
+     * never be drained and the pipeline freezes until the session dies.
+     * An unwritable client has its buffers SKIPPED (same loss semantics UDP
+     * multicast had — it was not reading them anyway) so a slow-STARTING
+     * consumer (seconds of VA/librist init between its connect and PLAYING)
+     * survives; only a client continuously unwritable for KICK_AFTER_US is
+     * assumed dead and kicked (it reconnects via its own restart path). */
+    if (type == COMMAND_TYPE_NEW_BUFFER) {
+      if (!g_socket_condition_check (socket, G_IO_OUT)) {
+        gint64 now = g_get_monotonic_time ();
+        if (client->blocked_since == 0) {
+          client->blocked_since = now;
+        } else if (now - client->blocked_since > KICK_AFTER_US) {
+          GST_WARNING_OBJECT (self,
+              "Client %p has not drained its socket for %d s — kicking dead consumer",
+              client, (int) (KICK_AFTER_US / G_USEC_PER_SEC));
+          g_hash_table_iter_remove (&iter);
+          client_removed = TRUE;
+          continue;
+        }
+        /* Skip this buffer for this client only. */
+        continue;
+      }
+      client->blocked_since = 0;
+    }
     if (!gst_unix_fd_send_command (socket, type, fds, payload, payload_size,
             &error)) {
       GST_ERROR_OBJECT (self, "Failed to send command %d to client %p: %s",
