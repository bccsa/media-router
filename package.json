{
    "name": "@media-router/root",
    "version": "2.0.0",
    "private": true,
    "description": "Media Router v2.0 — Distributed media routing system",
    "scripts": {
        "build": "pnpm -r --filter './packages/*' --filter './plugins/*' run build",
        "dev": "pnpm -r --parallel --filter @media-router/manager --filter @media-router/engine --filter @media-router/manager-ui --filter @media-router/local-panel --filter @media-router/profile-manager run dev",
        "dev:all": "pnpm -r --parallel --filter './packages/*' run dev",
        "test": "vitest run && pnpm --filter @media-router/engine run test:py && pnpm --filter @media-router/plugin-mpegts-core run test:py && pnpm --filter @media-router/plugin-aes67-core run test:py",
        "test:watch": "vitest",
        "typecheck": "pnpm -r --filter './packages/*' --filter './plugins/*' run typecheck",
        "lint": "prettier --check 'packages/*/src/**/*.{ts,vue}' 'plugins/*/engine/**/*.ts'",
        "format": "prettier --write 'packages/*/src/**/*.{ts,vue}' 'plugins/*/engine/**/*.ts'"
    },
    "devDependencies": {
        "@vitest/coverage-v8": "^3.2.4",
        "@vue/test-utils": "^2.4.6",
        "pinia": "^3.0.4",
        "prettier": "^3.5.3",
        "tsx": "^4.21.0",
        "typescript": "^5.9.3",
        "vitest": "^3.2.1",
        "vue": "^3.5.30"
    },
    "pnpm": {
        "onlyBuiltDependencies": [
            "better-sqlite3",
            "esbuild"
        ]
    }
}
