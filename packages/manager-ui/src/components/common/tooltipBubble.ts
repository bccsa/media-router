/**
 * Bubble styling shared by the two tooltip idioms — `MrTooltip` (CSS hover,
 * absolutely positioned inside non-scrolling chrome) and `MrHelpTip`
 * (teleported, fixed, for content inside scroll boxes and touch screens).
 * Position, width and font size stay with each component.
 */
export const TOOLTIP_BUBBLE_CLASS =
    'p-2 rounded-md shadow-lg leading-relaxed pointer-events-none bg-card border border-border text-foreground';
